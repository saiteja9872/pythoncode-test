pipeline {
  agent { label 'linux' }
  options {
  buildDiscarder(logRotator(numToKeepStr: '5'))
  }
  stages {
    stage('Scan') {
      steps {
        withSonarQubeEnv(installationName: 'sonarqube9090') {
          sh './mvnw clean org.sonarsource.scanner.maven:sonar-maven-plugin:3.9.02155:sonar'
         }
        }
       }
     }
 }
